- Initialized React + TypeScript app with Vite in `app/`.
- Installed `styled-components` and ESLint Prettier integration.
- Added `Navbar`, `ChallengeCard`, and `Home` page components.
- Implemented hero section, CTA, and three responsive challenge cards.
- Ensured accessibility with semantic HTML, focus states, and aria labels.
- Integrated components in `App.tsx` and removed starter content.
- Ran `pnpm run lint` and `pnpm run build`; both completed successfully. 